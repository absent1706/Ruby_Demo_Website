Hi, <%= @user.name %>!

To confirm your registration and automatically sign in click the link below.
<%= link_to "#{root_url}activate/#{@user.id}/#{@user.signup_confirm_token}", "#{root_url}activate/#{@user.id}/#{@user.signup_confirm_token}" %>

You have 7 days to complete registration.