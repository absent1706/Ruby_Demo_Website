Hi, <%= @user.name %>!

To confirm your registration and automatically sign in click the link below.
<%= link_to "localhost:3000/activate/#{@user.id}/#{@user.signup_confirm_token}", "localhost:3000/activate/#{@user.id}/#{@user.signup_confirm_token}" %>

You have 7 days to complete registration.